[!exec:git] skip 'git not available'

setup-repo
setup-project

cd $WORK/project

# --dry-run should not create the worktree
exec wtx add --dry-run --skip-setup main
stderr 'dry-run'
! exists worktrees/main
