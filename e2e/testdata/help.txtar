# wtx --help should succeed and show usage
exec wtx --help
stdout 'A smarter git worktree workflow'
stdout 'clone'
stdout 'add'
stdout 'list'

# wtx --version should succeed
exec wtx --version
stdout 'wtx version'
